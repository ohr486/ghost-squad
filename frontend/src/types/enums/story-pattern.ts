/**
 * Story pattern enumeration for template matching (renamed from TaskPattern)
 */
export enum StoryPattern {
  BUG_FIX = 'bug_fix',
  FEATURE_ADDITION = 'feature_addition',
  INVESTIGATION = 'investigation',
  REVIEW = 'review',
  MAINTENANCE = 'maintenance'
}