# 改善ポイントチェックリスト

このドキュメントは、プロジェクトの品質、セキュリティ、保守性を向上させるための改善点をチェックリスト形式でまとめたものです。

## １. 全体的なアーキテクチャと設計

- [ ] **エージェントロジックの具体化**: `apps/api/agents/graph.py` の `node_worker` がシミュレーションであり、実際のタスク処理を行っていない。Plannerが生成した計画(`current_plan`)に基づいて、具体的な処理（コード生成、ファイル操作など）を行うロジックを実装する。
- [ ] **リアルタイム更新への移行**: フロントエンドのポーリングを非効率なものから、WebSocketやServer-Sent Events (SSE) を用いたバックエンドからのプッシュ通知によるリアルタイム更新へ移行する。
- [ ] **DBマイグレーションの導入**: API起動時の `Base.metadata.create_all` ではなく、Alembicのようなマイグレーションツールを導入し、データベーススキーマの変更を安全に管理する。

## ２. セキュリティ

- [ ] **認証・認可の欠如**: APIエンドポイントにAPIキー、OAuth2、JWTなどを用いた認証・認可の仕組みを導入する。
- [ ] **設定とシークレットの管理**: `docker-compose.yml` にハードコードされているデータベースパスワードなどのシークレット情報を `.env` ファイルに分離し、Git管理対象から外す。FastAPIのCORS設定（`origins`）も環境変数で管理する。

## ３. コードの品質と保守性

- [x] **スキーマ定義の統一**: Pydanticモデル（`Task`, `PlanSchema`など）を `schemas.py` に一元化する。
- [ ] **バックグラウンドタスクの不完全な実装**: `/mission/start` エンドポイントでAIエージェント（`ghost_brain`）の呼び出しを `BackgroundTasks` を利用して非同期で行う。
- [ ] **型安全性の向上 (Frontend)**: フロントエンドのstate（`missionData`, `history`）の `any` 型を、バックエンドのPydanticスキーマからTypeScriptの型を自動生成するツール（例: `pydantic-to-typescript`）を用いて厳密な型に置き換える。
- [ ] **コンポーネントの分割 (Frontend)**: `page.tsx` を `MissionHistory`, `LogPanel`, `InputBar` のように、関心事ごとにコンポーネントを分割する。

## ４. UI/UXの改善

- [ ] **API URLの環境変数化**: フロントエンドのコード内でハードコードされているAPIのURLを、`docker-compose.yml`で定義されている `NEXT_PUBLIC_API_URL` を利用するように修正する。
- [ ] **ユーザー体験の向上**: 削除時の確認ダイアログをブラウザ標準の `confirm()` から、アプリのスタイルに合わせたカスタムモーダルに置き換える。

## ５. その他

- [ ] **不要なコードの削除**: `main.py` にある `/mission/reset` エンドポイントが不要であれば削除する。
- [ ] **エラーハンドリングの改善**: `graph.py` のPlannerノードでのLLM通信エラーハンドリングを、より具体的なもの（リトライ、エラーログ記録など）に改善する。
- [x] **テストコードの修正**: `make test` で失敗するテストを修正する。
- [x] **テストコードの不足をチェックし追加**: 不足しているテストコードを追加し、既存のテストの失敗を修正する。
- [x] **Lint警告の修正**: `make lint` で表示される警告を修正する。
- [ ] **テストランナーのタイムアウト**: `make test` を実行すると、APIテストはすべて成功するが、コマンドがタイムアウトする問題を調査する。
